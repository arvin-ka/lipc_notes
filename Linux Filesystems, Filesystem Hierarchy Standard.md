## پارتیشن بندی در لینوکس

در چرخه مدیریت دیسک در لینوکس، ترتیب منطقی به این صورت است:

 گام اول : ایجاد پارتیشن (fdisk / gdisk / parted)

 گام دوم : ساخت سیستم فایل / فرمت (mkfs)

 گام سوم : مانت کردن (mount / /etc/fstab)

 گام چهارم : بررسی و نگهداری (fsck / tune2fs)

 
## گام اول: مدیریت و ایجاد جداول پارتیشن (Partitioning)

پیش از اینکه بتوان روی یک دیسک خام (Raw Disk) سیستم فایل ایجاد کرد (`mkfs`)، ابتدا باید ساختار پارتیشن‌های آن مشخص شود. پارتیشن‌بندی باعث تقسیم‌بندی فیزیکی یا منطقی دیسک به بخش‌های مجزا می‌شود.

#### مقایسه جداول پارتیشن: MBR در برابر GPT

۱. **جدول پارتیشن MBR (Master Boot Record):**
   * **محدودیت حجم:** حداکثر از دیسک‌های تا **۲ ترابایت** پشتیبانی می‌کند.
   * **محدودیت تعداد:** حداکثر **۴ پارتیشن Primary** پشتیبانی می‌شود. برای ایجاد پارتیشن‌های بیشتر، باید یکی از آن‌ها به صورت **Extended** تعریف شده و درون آن پارتیشن‌های **Logical** ساخته شوند.
   * **شناسه کدهای رایج در fdisk:** کد `83` برای Linux Filesystem و کد `82` برای Linux Swap.

۲. **جدول پارتیشن GPT (GUID Partition Table):**
   * **محدودیت حجم:** پشتیبانی از دیسک‌های بسیار بزرگ (تا **۹.۴ زتابایت**).
   * **محدودیت تعداد:** پشتیبانی از **۱۲۸ پارتیشن مجزا** به‌صورت پیش‌فرض (بدون نیاز به پارتیشن Extended/Logical).
   * **پایداری بالا:** دارای یک نسخه پشتیبان از جدول پارتیشن در انتهای دیسک (Backup GPT Header) جهت بازیابی در صورت خرابی.

---

###  ابزارهای پارتیشن‌بندی دیسک

برای ساخت و ویرایش پارتیشن‌ها از ابزارهای مختلفی استفاده می‌شود:

* `fdisk`: ابزار تعاملی و قدیمی لینوکس (عمدتاً برای MBR، با پشتیبانی محدودتر از GPT).
* `gdisk`: ساختار و دستوراتی کاملاً مشابه fdisk اما اختصاصی برای دیسک‌های GPT.
* `parted`: ابزار قدرتمند پیشرفته که هم از MBR و هم GPT پشتیبانی می‌کند و قابلیت اجرا به صورت غیرتعاملی (اسکریپت‌نویسی) را دارد.
* `cfdisk` / `cgdisk`: واسط کاربری متنی و گرافیكی‌تر (ncurses) برای پارتیشن‌بندی راحت‌تر.

---

#### فرایند گام‌به‌گام پارتیشن‌بندی با ابزار fdisk


برای پارتیشن‌بندی یک دیسک جدید (مثلاً `/dev/sdb`) مراحل زیر انجام می‌شود:

```bash
### ۱. ورود به محیط تعاملی fdisk برای دیسک مورد نظر
fdisk /dev/sdb
```


### دستورات کلیدی داخل محیط تعاملی fdisk:

* p (Print): نمایش جدول پارتیشن‌های فعلی دیسک.
* n (New): ایجاد یک پارتیشن جدید.
* t (Type): تغییر نوع/کد پارتیشن (مثلاً تغییر به Swap یا EFI).
* d (Delete): حذف یک پارتیشن موجود.
* v (Verify): بررسی و صحت‌سنجی جدول پارتیشن.
* q (Quit): خروج از برنامه بدون ذخیره‌سازی تغییرات.
* w (Write): ذخیره نهایی تغییرات روی دیسک و خروج.

### نمونه مراحل ایجاد یک پارتیشن ۱۰ گیگابایتی:

1. کلید n را بزنید.
2. نوع پارتیشن را انتخاب کنید (p برای Primary یا e برای Extended).
3. شماره پارتیشن را انتخاب کنید (مثلاً 1).
4. بخش First Sector را با زدن Enter روی حالت پیش‌فرض بگذارید.
5. در بخش Last Sector حجم دلخواه را وارد کنید: 10G+
6. با کلید w تغییرات را روی دیسک رایت و ذخیره کنید.

### ایجاد پارتیشن به صورت اسکریپت‌پذیر با دستور parted

```
# ۱. تعیین نوع جدول پارتیشن دیسک به GPT
parted /dev/sdb mklabel gpt

# ۲. ساخت یک پارتیشن Primary با حجم ۵ گیگابایت
parted /dev/sdb mkpart primary ext4 1MiB 5GiB

# ۳. مشاهده وضعیت پارتیشن‌های دیسک
parted /dev/sdb print
```

### اعمال تغییرات به کرنیل بدون نیاز به Reboot (دستور partprobe)

وقتی جدول پارتیشن یک دیسک را تغییر می‌دهید، کرنیل سیستم‌عامل ممکن است بلافاصله متوجه پارتیشن‌های جدید (مثل ``/dev/sdb1``) نشود و در زمان فرمت با خطای Device or resource busy یا No such file or directory مواجه شوید.

برای شناساندن لحظه‌ای پارتیشن‌های جدید به کرنیل بدون ریستارت کردن سیستم، از دستور زیر استفاده می‌شود:

```
# به روزرسانی جدول پارتیشن‌ها در کرنیل
partprobe /dev/sdb

# یا شناسایی دیوایس‌های جدید
kpartx -a /dev/sdb
```




در لینوکس، پارتیشن‌بندی دیسک‌ها به تنهایی برای ذخیره‌سازی فایل‌ها کافی نیست. برای اینکه سیستم‌عامل بتواند داده‌ها را سازماندهی و ذخیره کند، باید روی هر پارتیشن یک سیستم فایل (Filesystem) یا فضای Swap ایجاد شود.

## گام دوم : سیستم فایل (Filesystem):
  
  فایل سیستم ساختاری دفتری و منطقی است که به سیستم‌عامل می‌گوید دیتاها چگونه روی حافظه ذخیره، دسته‌بندی، خوانده و مدیریت شوند.

  بدون فایل سیستم، هارد شما فقط یک توده خام از اطلاعات (Raw Data) است. سیستم‌عامل مجبور بود برای پیدا کردن یک عکس ساده، کل هارد را از صفر تا صد اسکن کند، چون نمی‌دانست عکس کجا ذخیره شده است!

  اگر هارد دیسک یا SSD را مثل یک کتابخانه کاملاً خالی و بزرگ تصور کنید، فایل سیستم مثل **قفسه‌بندی**، **زون‌بندی** و سیستم **کاتالوگ‌نویسی** آن کتابخانه است.

### 2.1 حافظه مجازی (Swap):

 یک فایل سیستم یا فضای ویژه که لینوکس از آن به عنوان **رم مجازی** استفاده می‌کند (وقتی رم اصلی پر شود، اطلاعات روی Swap منتقل می‌شوند).

### 2.2 سیستم‌های فایل رایج در لینوکس

کتاب مرجع LPIC-1 سیستم‌های فایل زیر را پوشش می‌دهد:

| سیستم فایل | توضیحات و ویژگی ها |
| :--- | :--- |
ext2 | قدیمی‌تر است و قابلیت Journaling (دفترچه ثبت تغییرات) ندارد. به همین دلیل اگر سیستم ناگهانی خاموش شود، احتمال آسیب به داده‌ها بالاست. امروز بیشتر برای حافظه‌های فلش یا پارتیشن‌های کوچک boot که نیازی به ثبت تغییرات ندارند استفاده می‌شود.
ext3 | همان ext2 است که قابلیت Journaling به آن اضافه شد تا در صورت قطعی برق، داده‌ها آسیب نبینند.
ext4 | سیستم فایل پیش‌فرض استاندارد در اکثر توزیع‌های لینوکسی؛ پشتیبانی از حجم‌های بسیار بالا، عملکرد بهتر و قابلیت تخصیص بلاک Extents.
XFS | سیستم فایل بسیار سریع و مقیاس‌پذیر، مناسب برای فایل‌های حجم بالا و سرورها (سیستم فایل پیش‌فرض در توزیع‌های مبتنی بر RHEL/CentOS).
Btrfs | سیستم فایل مدرن مبتنی بر Copy-on-Write (CoW) با قابلیت‌های پیشرفته مانند Snapshot، فایل‌سیستم یکپارچه و RAID نرم‌افزاری.
VFAT / FAT32 / exFAT | سیستم‌های فایل شرکت مایکروسافت برای سازگاری کامل بین لینوکس، ویندوژ و دیگر سیستم‌عامل‌ها (به‌ویژه در حافظه‌های جانبی و پارتیشن EFI).

### 2.3 توضیح قابلیت journaling : 

فایل سیستم‌های مدرن یک «دفترچه ثبت تغییرات» (Journal) دارند. اگر ناگهان برق برود، سیستم با خواندن این دفترچه می‌فهمد چه عملیاتی نیمه‌کاره مانده تا از خراب شدن فایل‌ها جلوگیری کند.


#### مقایسه با ویندوز

برای درک بهتر : 

 **ویندوز** معمولاً از فایل سیستم‌های NTFS و FAT32 / exFAT استفاده می‌کند.

 **لینوکس** به‌طور نیتیو روی ext4 یا XFS نصب می‌شود، اما می‌تواند پارتیشن‌های NTFS و FAT32 ویندوز را هم بخواند و بنویسد.

 


### 2.4 نحوه ساخت سیستم‌های فایل (Formatting)

برای ایجاد فایل‌سیستم روی یک پارتیشن، از دستور کلی ``mkfs`` (مخفف Make Filesystem) استفاده می‌شود.

### استفاده از دستور mkfs

این دستور می‌تواند همراه با سوییچ ``-t`` نوع سیستم فایل را مشخص کند:

```
mkfs -t ext4 /dev/sdb1
mkfs -t xfs /dev/sdb2
mkfs -t vfat /dev/sdc1
```

### ابزارهای اختصاصی ساخت فایل‌سیستم

```
# ساخت سیستم فایل ext2/ext3/ext4
mkfs.ext2 /dev/sdb1
mkfs.ext3 /dev/sdb1
mkfs.ext4 /dev/sdb1

# ساخت سیستم فایل XFS
mkfs.xfs /dev/sdb2

# ساخت سیستم فایل VFAT/FAT32
mkfs.vfat /dev/sdc1
```

### تنظیم ویژگی‌های پیشرفته در سیستم‌های ext (mke2fs)

دستور mke2fs ابزار اصلی زیربنایی برای خانواده ext است و سوییچ‌های مهمی دارد:

```
# ایجاد سیستم فایل ext4
mke2fs -t ext4 /dev/sdb1

# اختصاص Label (نام) به پارتیشن با سوییچ -L
mke2fs -t ext4 -L "DataDrive" /dev/sdb1

# رزرو درصدی از بلاک‌ها برای کاربر root (پیش‌فرض ۵٪ است) با سوییچ -m
mke2fs -t ext4 -m 2 /dev/sdb1     # کاهش میزان رزرو به ۲ درصد

# تنظیم تعداد Inodeها یا ابعاد بلاک‌ها
mke2fs -t ext4 -b 4096 /dev/sdb1  # تنظیم اندازه بلاک به 4KiB
```

### 2.5 مدیریت و پیکربندی فضاهای Swap

**فضای Swap به دو صورت قابل ایجاد است: پارتیشن مجزا (Swap Partition) یا فایل Swap (Swap File).**

روش اول: ایجاد Swap روی پارتیشن

1. ابتدا نوع پارتیشن را در ``fdisk`` یا ``gdisk`` روی کد ``82`` (Linux Swap) قرار دهید.
2. با دستور ``mkswap`` ساختار Swap را روی پارتیشن اعمال کنید:

```
mkswap /dev/sdb3
# می‌توانید به آن Label اختصاص دهید:
mkswap -L "SWAP1" /dev/sdb3
```

3. فعال‌سازی فضای Swap:

`` swapon /dev/sdb3 ``

### روش دوم: ایجاد Swap با استفاده از Swap File

اگر روی دیسک پارتیشن خالی ندارید، می‌توانید یک فایل مجزا ایجاد کرده و از آن به عنوان Swap استفاده کنید:

```
# ۱. ایجاد یک فایل به حجم ۱ گیگابایت با دستور dd
dd if=/dev/zero of=/swapfile bs=1M count=1024

# ۲. ایمن‌سازی دسترسی‌ها (فقط کاربر root باید به فایل دسترسی داشته باشد)
chmod 600 /swapfile

# ۳. تبدیل فایل به Swap
mkswap /swapfile

# ۴. فعال‌سازی Swap File
swapon /swapfile
```

### دستورات مدیریت و بررسی وضعیت Swap

  **مشاهده وضعیت حافظه و Swap فعال:**

  ```
free -h
# یا مشاهده از طریق /proc
cat /proc/swaps
swapon --show
```
  **غیرفعال‌سازی فضای Swap:**

  ```
swapoff /dev/sdb3
# یا برای غیرفعال کردن فایل swap:
swapoff /swapfile
```

### 2.6 ابزارهای بررسی و سفارشی‌سازی فایل‌سیستم (tune2fs)

دستور ``tune2fs`` امکان تغییر مشخصات سیستم‌های فایل خانواده ``ext`` را بدون نیاز به فرمت مجدد فراهم می‌کند:

```
# تغییر یا مشاهده Label پارتیشن
tune2fs -L "Backup_Drive" /dev/sdb1

# تغییر تعداد دفعات مانت (Mount Count) برای اجبار به کنترل سلامت (fsck)
tune2fs -c 30 /dev/sdb1

# تنظیم بازه زمانی برای کنترل اجباری (مثلاً هر ۳ ماه)
tune2fs -i 3m /dev/sdb1

# تبدیل سیستم فایل ext2 به ext3 (اضافه کردن Journal)
tune2fs -j /dev/sdb1

# مشاهده کامل مشخصات و متادیتاهای سیستم فایل (Superblock)
tune2fs -l /dev/sdb1
```

پس از ساخت سیستم‌های فایل و پارتیشن‌های Swap (که در پارت قبلی بررسی شد)، دو مرحلهٔ حیاتی دیگر برای مدیریت حافظه در لینوکس وجود دارد:

1. بررسی، عیب‌یابی و حفظ یکپارچگی سیستم‌های فایل (Maintain Integrity)
2. اتصال (Mounting) و قطع اتصال (Unmounting) سیستم‌های فایل به درختِ یکپارچهٔ دایرکتوری‌ها

در لینوکس هیچ درایوی مانند ``C:`` یا ``D:`` به شکل مستقیم و مستقل وجود ندارد؛ بلکه تمام سیستم‌های فایل و پارتیشن‌ها باید به یک دایرکتوری در شجره‌نامهٔ ریشه (``/``) وصل (Mount) شوند.



### بررسی و تعمیر سیستم فایل (Filesystem Integrity)

به دلایلی مانند قطعی ناگهانی برق، خاموش شدن غیرمنتظره سیستم یا مشکلات سخت‌افزاری دیسک، ممکن است ساختار متادیتا و Inodeهای سیستم فایل دچار ناهمگونی یا خرابی شود.

### ابزار fsck (Filesystem Check)

دستور ``fsck`` ابزار مرجع برای بررسی و بازسازی سیستم‌های فایل است. این دستور در واقع فرادستوری است که ابزارهای خاص هر فایل‌سیستم (مانند ``e2fsck`` برای خانواده ext) را در پس‌زمینه اجرا می‌کند.

**نکته بسیار مهم: هرگز دستور ``fsck`` را روی پارتیشنِ مانت‌شده (Mounted) اجرا نکنید! انجام این کار می‌تواند باعث آسیب شدید و از دست رفتن داده‌ها شود. ابتدا پارتیشن را ``umount`` کنید.**

```
# بررسی کلی سیستم فایل
fsck /dev/sdb1

# بررسی اجباری تمام سیستم‌های فایل ذکر شده در /etc/fstab (معمولاً در هنگام بوت سیستم)
fsck -A

# تعمیر خودکار و بدون پرسش تمامی خطاهای پیدا شده
fsck -a /dev/sdb1
# یا
fsck -y /dev/sdb1
```

### ابزارهای اختصاصی عیب‌یابی

برای نمونه e2fsck: ابزار اختصاصی بررسی و تعمیر سیستم‌های فایل ext2 / ext3 / ext4:

```
e2fsck -f /dev/sdb1    # اجبار به بررسی (Force check) حتی اگر پاک پاک به نظر برسد
e2fsck -p /dev/sdb1    # تعمیر خودکار بدون دخالت کاربر (Preen)
```

یک نمونه دیگر xfs_repair: ابزار اختصاصی برای بررسی و تعمیر سیستم فایل XFS:

`` xfs_repair /dev/sdb2 ``

### مشاهده مشخصات سیستم فایل با dumpe2fs

دستور ``dumpe2fs`` اطلاعات کاملی از Superblock و Superblock Backupها و وضعیت گروه بلاک‌ها در سیستم‌های فایل ext نمایش می‌دهد:

```
dumpe2fs /dev/sdb1
dumpe2fs -h /dev/sdb1    # فقط نمایش اطلاعات Superblock (Header)
```

## گام سوم : فرایند مانت و آن‌مانت کردن (Mounting & Unmounting)

برای دسترسی به محتویات یک پارتیشن یا سیستم فایل، باید آن را به یک نقطهٔ اتصال (Mount Point) وصل کرد.

### دستور mount

ساختار کلی دستور:

mount [-t filesystem_type] [-o option] device mount_point

```
# مانت کردن ساده یک پارتیشن
mount /dev/sdb1 /mnt/data

# تعیین صریح نوع سیستم فایل
mount -t ext4 /dev/sdb1 /mnt/data

# مانت کردن یک فایل ISO (Loop Device)
mount -o loop /path/to/image.iso /mnt/iso

# مانت کردن به‌صورت فقط‌خواندنی (Read-Only)
mount -o ro /dev/sdb1 /mnt/data

# دوباره مانت کردن پارتیشن با گزینه‌های جدید (Remount)
mount -o remount,rw /mnt/data
```

### دستور umount

برای جدا کردن سیستم فایل از نقطهٔ اتصال استفاده می‌شود. می‌توانید نام دیوایس یا مسیر Mount Point را وارد کنید:

```
umount /mnt/data
# یا
umount /dev/sdb1
```

#### رفع ارور "Target is busy":

اگر کاربری یا پردازش کامپیوتر در حال استفاده از دایرکتوری مانت‌شده باشد، امکان Unmount وجود ندارد. برای شناسایی و رفع آن:

```
# پیدا کردن پردازش‌های در حال استفاده از نقطه مانت
lsof /mnt/data
# یا
fuser -v /mnt/data

# قطع اجباری (در صورت نیاز)
fuser -km /mnt/data
```
### 3.1 پیکربندی پایدار و خودکار با /etc/fstab

فایل /``etc/fstab`` (File System Table) اصلی‌ترین فایل کانفیگ برای تعیین پارتیشن‌ها و سیستم‌های فایلی است که باید هنگام بوت شدن سیستم به‌صورت خودکار مانت شوند.

### ساختار هر سطر در فایل /etc/fstab:

این فایل شامل ۶ ستون (Field) است که با فاصله یا Tab از هم جدا شده‌اند:

```
[1. Device / UUID]  [2. Mount Point]  [3. FSType]  [4. Options]  [5. Dump]  [6. Fsck Pass]
```

1. اول Device / Identifier: شناسه پارتیشن که می‌تواند مسیر دیوایس (``/dev/sdb1``)، شناسه منحصربه‌فرد (``UUID=...``) یا لیبل (``LABEL=...``) باشد. (استفاده از UUID استانداردتر و ایمن‌تر است).
2. دوم Mount Point: مسیر دایرکتوری مقصد (مثلاً ``/`` یا ``/home`` یا ``none`` برای Swap).
3. سوم FSType: نوع سیستم فایل (``ext4``, ``xfs``, ``vfat``, ``swap``, ``auto``).
4. چهارم Options: آپشن‌های مانت که با کاما جدا می‌شوند (مانند ``defaults``, ``ro``, ``rw``, ``noexec``, ``nosuid``, ``user``).
5. پنجم Dump: تعیین اولویت پشتیبان‌گیری با دستور ``dump`` (``0`` یعنی بدون بکاپ، ``1`` برای فایل‌سیستم‌های اصلی).
6. ششم Fsck Pass: اولویت بررسی سلامت دیسک با ``fsck`` در زمان بوت:

   ``0``: بدون بررسی.
   
   ``1``: اولویت اول (مخصوص پارتیشن ریشه /).
   
   ``2``: اولویت دوم (برای بقیه پارتیشن‌ها).
   
### نمونه سطر در /etc/fstab:

```
UUID=4c8a221f-0e9b-46a2-a9b1-123456789abc  /home  ext4  defaults  0  2
```

**تست صحّت تغییرات fstab:**

**پس از دستکاری ``/etc/fstab`` حتماً دستور زیر را بزنید تا از صحّت فایل اطمینان حاصل کنید و سیستم در بوت بعدی دچار خطای Kernel Panic یا Emergency Mode نشود:**

`` mount -a ``

این دستور تمام ورودی‌های داخل ``/etc/fstab`` را که مانت نشده‌اند، مانت می‌کند.


### مشاهده و شناسایی سیستم‌های فایل و UUIDها

برای استخراج مشخصات و UUID پارتیشن‌ها می‌توان از ابزارهای زیر استفاده کرد:

``lsblk``: نمایش ساختار بلوکی دیسک‌ها به همراه نقاط مانت و UUIDها:

`` lsblk -f ``

``blkid``: نمایش مشخصات، Label و UUID دقیق همه دیوایس‌ها:

```
blkid
blkid /dev/sdb1
```

``df`` (Disk Free): نمایش میزان فضای پر و خالی سیستم‌های فایل مانت‌شده:

```
df -h    # به شکل خوانا برای انسان (Human Readable)
df -T    # نمایش نوع سیستم فایل هر پارتیشن
```

### 3.2 استاندارد سلسله‌مراتب فایل‌سیستم (FHS - Filesystem Hierarchy Standard)

در لینوکس تمام فایل‌ها، دایرکتوری‌ها و حتی سخت‌افزارها زیرمجموعه یک ریشه یگانه به نام ( `/` ) قرار می‌گیرند. استاندارد FHS وظیفه و محتوای هر دایرکتوری را در تمام توزیع‌های لینوکس مشخص می‌کند تا ساختاری یکپارچه ایجاد شود.

#### تشریح دایرکتوری‌های اصلی لینوکس:

* `/` (Root): ریشه اصلی درخت سیستم فایل که تمام دایرکتوری‌ها از آن شاخه می‌گیرند.
* `/boot`: فایل‌های حیاتی برای بوت شدن سیستم شامل فایل کرنل لینوکس (`vmlinuz`)، تصویر اولیه فایل‌سیستم (`initramfs`) و پیکربندی‌های Bootloader (مانند GRUB).
* `/etc`: تمامی فایل‌های متنی پیکربندی (Configuration Files) کل سیستم‌عامل و برنامه‌های نصب‌شده (مانند `/etc/fstab` یا `/etc/passwd`).
* `/var`: داده‌های متغیر که در طول زمان حجم آن‌ها تغییر می‌کند؛ مانند فایل‌های لاگ سیستم (`/var/log`)، صف ایمیل‌ها و فایل‌های دیتابیس.
* `/usr`: شامل برنامه‌ها، دستورات و کتابخانه‌های مربوط به کاربران (User Binaries & Libraries). بیشتر ابزارهای اجرایی سیستم‌عامل در `/usr/bin` و `/usr/sbin` قرار دارند.
* `/opt`: محل قرارگیری و نصب برنامه‌های جانبی و ثالث (Third-party Add-on Software) که خارج از مخازن اصلی توزیع نصب می‌شوند.
* `/dev`: فایل‌های دیوایس (Device Files) که نماینده سخت‌افزارهای متصل به سیستم هستند (مانند `/dev/sda` برای هارد دیسک یا `/dev/null`).
* `/proc` و `/sys`: فایل‌سیستم‌های مجازی (Pseudo-filesystems) که وجود فیزیکی روی دیسک ندارند و مستقیم در RAM ایجاد می‌شوند تا اطلاعات لحظه‌ای کرنیل و سخت‌افزار را نشان دهند.
* `/media`: نقطه مانت خودکار (Automatic Mount Point) برای رسانه‌های قابل حمل متصل‌شده مانند فلش‌مموری‌ها و CD/DVD.
* `/mnt`: نقطه مانت سنتی برای مانت کردن دستی و موقت پارتیشن‌ها توسط ادمین سیستم.
* `/tmp`: محل ذخیره‌سازی فایل‌های موقت برنامه‌ها که معمولاً با هر بار ریستارت سیستم پاک می‌شوند.
* `/home`: دایرکتوری خانگی کاربران معمولی جهت نگهداری فایل‌های شخصی.
* `/root`: دایرکتوری خانگی کاربر ارشد (Superuser/root).

---

### 3.3 مفاهیم Inode، آپشن‌های پیشرفته Mount و دستورات تکمیلی

#### مفهوم Inode (Index Node) و مدیریت آن

هر فایل یا دایرکتوری در لینوکس دارای یک ساختار متادیتایی به نام **Inode** است. Inode تمام اطلاعات و مشخصات فایل را نگه می‌دارد، از جمله:
* مالک فایل (UID) و گروه آن (GID)
* مجوزهای دسترسی (Permissions)
* اندازه فایل (File Size) و زمان‌های ویرایش/دسترسی (Timestamps)
* اشاره‌گرها به بلاک‌های فیزیکی دیسک که دیتای واقعی روی آن‌ها نوشته شده است.

> **نکته بسیار مهم:** **نام فایل** داخل Inode ذخیره نمی‌شود! نام فایل‌ها درون دایرکتوریِ والد نگهداری می‌شود و به شماره Inode آن فایل اشاره می‌کند.

```bash
# مشاهده شماره Inode یک فایل با سوییچ -i
ls -i /etc/passwd

# مشاهده درصد مصرف Inodeهای پارتیشن‌ها (اگر این میزان ۱۰۰٪ شود، امکان ایجاد فایل جدید وجود ندارد)
df -i
