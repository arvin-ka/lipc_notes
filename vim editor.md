### بخش ۱: چرا یادگیری Vim ضروری است؟

ویرایشگر vi (و نسخه پیشرفته‌تر آن vim) یکی از ابزارهای حیاتی در مدیریت سیستم‌های مبتنی بر لینوکس/یونیکس است. سه علت اصلی وجوب یادگیری آن عبارتند از:

1. **حضور همه‌جا حاضری (Ubiquity)**: در تقریباً تمام توزیع‌های لینوکس و سیستم‌عامل‌های خانواده Unix/BSD به‌صورت پیش‌فرض نصب است.
2. در دسترس بودن در شرایط اضطراری: هنگام خرابی سیستم (Rescue Mode)، بوت نشدن GUI، یا در Minimal Installها، معمولاً تنها ادیتوری که در دسترس است vi است.
3. سرعت و قدرت فوق‌العاده بالا: بدون نیاز به ماوس، تمامی کارهای ویرایش، حرکت، جایگزینی و جستجو از طریق کیبورد با سرعتی بی‌نظیر انجام می‌شود.
 
**نکته: در اکثر سیستم‌عامل‌های امروزی، اجرای دستور vi در واقع یک Symbolic Link به vim (Vi IMproved) است.**

### بخش ۲: درک حالت‌های کاری (Modes) در Vim

برعکس ادیتورهای معمولی، Vim یک ادیتور Modal (مبتنی بر حالت) است. سه حالت اصلی آن عبارتند از:


```
               +-----------------------+
               |     Normal Mode       | <--- (پیش‌فرض با Esc)
               +-----------------------+
                /                     \
      فشار کلید i, a, o          فشار کلید : esc
              /                         \
             v                           v
+-----------------------+     +-----------------------+
|      Insert Mode      |     |  Command-line Mode    |
| (تایپ متن در فایل)   |     | (ذخیره، خروج، جستجو)  |
+-----------------------+     +-----------------------+
```

1. اول Normal Mode (حالت معمولی): حالت پیش‌فرض جهت پیمایش، کپی/پست، حذف و اجرای کلیدهای میانبر.
2. دوم Insert Mode (حالت درج/ویرایش): برای تایپ و وارد کردن متن جدید.
3. سوم Command-line Mode (حالت دستور): برای دستورات ذخيره‌سازی، خروج، جستجو و تنظیمات ادیتور (با تایپ : شروع می‌شود).

### بخش ۳: راهنمای کامل کلیدها و دستورات Vi / Vim

#### سوئیچ بین حالت‌ها (Entering Insert Mode)

از حالت Normal برای ورود به Insert Mode از کلیدهای زیر استفاده می‌شود:

| کلید | کاربرد |
| :--- | :--- |
| ``i`` | ورود به حالت insert **قبل از مکان نما**
| ``I`` | ورود به حالت insert **در ابتدای خط  فعلی**
| ``a`` | ورود به حالت Insert **بعد از مکان نما**
| ``A`` | ورود به حالت Insert **در انتهای خط فعلی**
| ``o`` | باز کردن **خط جدید در زیر** خط فعلی و ورود به حالت insert
| ``O`` | باز کردن **خط جدید در بالای** خط فعلی و ورود به insert

### کلیدهای پیمایش و حرکت (Navigation)

| کلید | جهات اصلی | کلید | پیمایش سریع |
| :--- | :--- | :--- | :---
|``h``|حرکت به چپ|``w``|پرش به کلمه بعدی
|``j``|حرکت به پایین|``b``|پرش به کلمه قبلی
|``k``|حرکت به بالا|``0``|رفتن به ابتدای خط
|``l``|حرکت به راست|``$``|رفتن به انتهای خط
|``gg``|رفتن به اولین خط فایل|``G``|رفتن به آخرین خط فایل 
|``:n / nG`` | رفتن به خط شماره n

### ویرایش، حذف، کپی و چسباندن

| دستور | عملکرد
| :--- | :--- |
| ``x``| حذف کاراکتر زیر مکان‌نما
| ``dd`` | حذف (Cut) کامل خط فعلی
| ``3dd`` | حذف ۳ خط متوالی 
| ``dw`` | حذف از مکان‌نما تا پایان کلمه
| ``yy / Y`` | کپی کردن (Yank) کامل خط فعلی
| ``3yy`` | کپی کردن ۳ خط متوالی
| ``p`` | چسباندن (Paste) در خط زیرین / بعد مکان‌نما
| ``P`` |چسباندن در خط بالایی / قبل مکان‌نما
| ``u`` | بازگردانی (Undo)
| ``Ctrl + r`` | انجام مجدد (Redo)

